import { Button, Card } from '@mui/material';
import React from 'react';

type Props = {};

const MUIComp = (props: Props) => {
  return (
    <>
      <Card>s</Card>
      <Button variant='contained'>Hello World</Button>
    </>
  );
};

export default MUIComp;
